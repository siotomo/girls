name: ESLint Test
run-name: ${{ github.actor }} is testing out ESLint 🚀
on: [push]
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn install
      - uses: technote-space/get-diff-action@v6
        with:
          PATTERNS: |
            app/frontend/js/**/*.+(ts|tsx)
          DIFF_FILTER: 'ACMR'
      - uses: reviewdog/action-eslint@v1
        with:
          eslint_flags: 'app/frontend/js/components/girls/Detail.tsx'
      - run: echo ${{ env.GIT_DIFF_FILTERED }}